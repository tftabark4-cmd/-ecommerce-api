# SECURITY_REVIEW.md
## المراجعة الأمنية الأولية — Ecommerce API

تاريخ المراجعة: 2026-08-10
النطاق: مراجعة كود المشروع الحالي (Node.js / Express / Neon PostgreSQL) قبل تطبيق أي حماية.

---

### 1. عدم وجود Authentication أو Authorization إطلاقًا

- **المكان:** `src/app.js`, `src/routes/*.js` (جميع المسارات)
- **الخطر:** لا يوجد أي تسجيل دخول، لا JWT، ولا أي وسيط (middleware) يتحقق من هوية المستخدم. أي شخص يقدر يستدعي أي Endpoint — بما فيها إنشاء/تعديل المنتجات والمستخدمين — دون أي قيد.
- **مستوى الخطورة:** **حرج (Critical)** — لا يوجد أي حاجز بين المستخدم المجهول وبيانات النظام الإدارية.
- **المعالجة المقترحة:** بناء نظام Authentication كامل باستخدام JWT (`register`, `login`, `me`) ثم إضافة Authorization بأدوار `customer` و`admin` وتطبيقها على المسارات الحساسة.

---

### 2. استقبال `password_hash` مباشرة من العميل عند إنشاء مستخدم

- **المكان:** `src/controllers/usersController.js` — دالة `createUser` (السطر 56-69)
- **الخطر:** الـ Endpoint `POST /api/users` يستقبل حقل `password_hash` جاهزًا من جسم الطلب ويخزّنه كما هو في قاعدة البيانات، دون أي تجزئة (hashing) فعلية من طرف الخادم. هذا يعني أن العميل يتحكم بالكامل بما يُخزَّن، وأن كلمات المرور قد تُرسَل وتُخزَّن كنص واضح إذا لم يقم العميل بتجزئتها بنفسه، وأيضًا لا يوجد أي تحقق من قوة كلمة المرور.
- **مستوى الخطورة:** **حرج (Critical)** — يهدد سرية بيانات اعتماد جميع المستخدمين.
- **المعالجة المقترحة:** استقبال `password` نصي فقط من العميل، تطبيق حد أدنى لطول كلمة المرور، وتوليد `password_hash` باستخدام `bcrypt` داخل الخادم حصرًا. هذا الحقل يجب ألا يظهر أبدًا في أي استجابة API.

---

### 3. عدم تفعيل معالج الأخطاء المركزي (Error Handler) الموجود فعليًا

- **المكان:** `src/middleware/errorHandler.js` (الملف موجود لكنه غير مستورد أو مستخدم في `src/app.js`)
- **الخطر:** كل Controller يكتب معالجة الأخطاء بشكل منفصل ومكرر يدويًا (`catch` blocks متفرقة)، ولا يوجد أي نقطة مركزية موحّدة. هذا يزيد احتمال نسيان معالجة خطأ ما في مسار جديد مستقبلًا، وقد يؤدي لاحقًا لتسريب تفاصيل داخلية (Stack Trace أو تفاصيل استعلام SQL) في حال نسيان `catch` بأحد المسارات.
- **مستوى الخطورة:** متوسط (Medium)
- **المعالجة المقترحة:** استيراد `errorHandler` واستخدامه كآخر middleware في `app.js`، مع إضافة `notFound` middleware مخصص، وتوحيد شكل استجابة الخطأ بدون تفاصيل تقنية داخلية.

---

### 4. إعداد CORS مفتوح بالكامل لجميع المصادر

- **المكان:** `src/app.js` (السطر 10) — `app.use(cors());`
- **الخطر:** استدعاء `cors()` بدون أي إعدادات يسمح لأي موقع (Origin) على الإنترنت بإرسال طلبات إلى الـ API والتفاعل معه من متصفح المستخدم، ما يفتح الباب أمام هجمات مثل CSRF من مواقع خبيثة عند إضافة نظام مصادقة قائم على الكوكيز أو استغلال الـ API من واجهات غير مصرح بها.
- **مستوى الخطورة:** متوسط إلى مرتفع (Medium-High) — يزداد خطره بعد إضافة Authentication.
- **المعالجة المقترحة:** تحديد قائمة `origin` مسموحة صراحة (مثل `http://localhost:5173` في بيئة التطوير) عبر متغير بيئة، وتحديد `methods` و`allowedHeaders` بدقة.

---

### 5. غياب Input Validation منظم على جميع المسارات

- **المكان:** `src/controllers/usersController.js`, `src/controllers/productsController.js`, `src/controllers/categoriesController.js`
- **الخطر:** التحقق من المدخلات يتم يدويًا وبشكل جزئي فقط (مثل التحقق من وجود الحقل أو أن السعر أكبر من صفر)، دون أي تحقق من صيغة البريد الإلكتروني، طول النصوص، نوع البيانات بدقة، أو القيم المسموحة للحقول مثل `role`. مثال: `updateProduct` لا يتحقق إطلاقًا من صحة القيم المرسلة قبل تنفيذ استعلام `UPDATE`.
- **مستوى الخطورة:** مرتفع (High)
- **المعالجة المقترحة:** استخدام مكتبة تحقق موحدة (`Zod` أو `express-validator`) على مستوى كل Endpoint يستقبل بيانات، مع إعادة استجابة موحدة توضح الحقول غير الصحيحة.

---

### 6. عدم وجود Rate Limiting

- **المكان:** `src/app.js` (لا يوجد أي استخدام لـ `express-rate-limit` أو ما شابه)
- **الخطر:** لا يوجد أي حد لعدد الطلبات المسموح بها من نفس المصدر، ما يسمح بإساءة استخدام الـ API (مثل محاولات تخمين كلمات مرور مستقبلية بعد إضافة تسجيل الدخول، أو إغراق الخادم بطلبات).
- **مستوى الخطورة:** متوسط (Medium)
- **المعالجة المقترحة:** إضافة حد عام لكل الـ API، وحد أكثر صرامة خصيصًا على مسار تسجيل الدخول لاحقًا.

---

### 7. عدم وجود Security Headers (Helmet)

- **المكان:** `src/app.js` (لا يوجد أي استخدام لمكتبة `helmet`)
- **الخطر:** الاستجابات لا تحتوي على ترويسات حماية أساسية مثل `X-Content-Type-Options` أو `Strict-Transport-Security`، ما يزيد من قابلية التطبيق للتعرض لهجمات معروفة (Clickjacking، MIME sniffing، إلخ) عند استخدامه من واجهة أمامية.
- **مستوى الخطورة:** منخفض إلى متوسط (Low-Medium)
- **المعالجة المقترحة:** تفعيل `helmet()` كأول middleware قبل تعريف المسارات.

---

## ملخص سريع

| # | المشكلة | الخطورة |
|---|---------|----------|
| 1 | لا يوجد Authentication/Authorization | حرج |
| 2 | استقبال password_hash من العميل مباشرة | حرج |
| 3 | errorHandler موجود لكنه غير مفعّل | متوسط |
| 4 | CORS مفتوح للجميع | متوسط-مرتفع |
| 5 | غياب Input Validation منظم | مرتفع |
| 6 | لا يوجد Rate Limiting | متوسط |
| 7 | لا يوجد Helmet / Security Headers | منخفض-متوسط |

ملاحظة: هذه المراجعة أولية وتغطي الحد الأدنى المطلوب (5 مشاكل)، وتم توثيق 7 لتغطية أوسع. سيتم إصلاح كل مشكلة ضمن التاسك المخصص لها لاحقًا، وتوثيق النتيجة قبل/بعد في تقرير الاختبار النهائي.
